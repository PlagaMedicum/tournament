version: 2

defaults: &defaults
  working_directory: /tournament
  docker:
    - image: circleci/golang:1.12-stretch

jobs:
  test:
    <<: *defaults
    steps:
      - checkout
      - run:
          name: deploy
          command: ./deploy.sh -l -t

workflows:
  version: 2
  test:
    jobs:
      - test
